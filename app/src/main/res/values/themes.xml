<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.ImlaApps" parent="android:Theme.Material.Light.NoActionBar" />

    <!-- Tema khusus untuk SplashActivity -->
    <style name="Theme.AlImla.SplashScreen" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Background splash -->
        <item name="android:windowBackground">@drawable/background</item>
        <!-- Hilangkan title bar -->
        <item name="android:windowNoTitle">true</item>
        <!-- Tampilkan dalam full screen -->
        <item name="android:windowFullscreen">true</item>
    </style>
</resources>